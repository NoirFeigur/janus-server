# Janus Server 开发进度

> 服务端开发进度与里程碑路线图。本文档是**执行视角的真相源**——记录「已落地什么、
> 哪里有缺口、接下来按什么顺序补」。架构取舍见 [`统一AI网关后台-架构决策.md`](./统一AI网关后台-架构决策.md)（**v1.5**），
> 表结构见 [`统一AI网关后台-数据模型设计.md`](./统一AI网关后台-数据模型设计.md)（**v0.5.5**），二者是**设计真相源**。
>
> **本文档的纪律**：① 只写**核实过的代码现状**，不抄设计文档的「应该有」当「已经有」；
> ② 每个里程碑分「✅ 已落地 / ⚠️ 缺口 / 📁 关键文件」三段，缺口必须显式列出，不许用 ✅ 掩盖；
> ③ 「后台管理系统基本盘」单列一章按项打勾，缺的就标缺；④ 设计文档与代码出现漂移时，本文档记录并指向待同步项。
>
> **更新时间**：2026-06-23 M2 LLM 网关热路径完成：三协议原生直通（Anthropic `aanthropic_messages` / Gemini `agenerate_content_stream` / OpenAI `acompletion`）+ Lua 原子配额 + 用量记账 + 772 passed / 2 skipped，ruff clean。
>
> **上一轮（2026-06-22）**：单元测试覆盖率补全轮：新增 52 个 service 级测试（641→693 passed），整体覆盖率 96%→98%；核实后台基本盘四项——统一分页响应 ✅、批量操作 ✅、国际化资源管理 ✅（按"后端只发 code"设计已满足）、个人中心仅缺 sk-key 自助入口（留 M3）。
>
> **上一轮（2026-06-18 OSS 附件基建）**：私有桶对象存储封装 `core/oss.py` + 头像 webp 转码/剥 EXIF `core/image.py` + 统一上传接口 `attach/` + 用户头像自助绑定 + `/auth/me` 预签名 `avatar_url` + 管理员代重置密码。全量测试 513 passed。真实 MinIO 端到端 QA 通过。
>
> **再上一轮（2026-06-18 Wave A/C/B/D）**：列表体验基建（统一查询过滤/排序 + 批量操作 + 字段脱敏）；审计日志（操作日志 + 登录日志）；会话安全（logout/refresh 轮换/令牌吊销/在线用户/登录锁定 + IP 限流/密码强度 + 强制登出）；平台配置（sys_config 键值表 + 类型化短 TTL 缓存读取）。全量测试 474 passed。

---

## 〇、系统全景（30 秒回顾，细节见架构稿）

公司统一内部 AI 基础设施平台，一个模块化单体（FastAPI），纳管两类 AI 能力：

```
客户端（Hermes / Claude Code / OA / 脚本）
   │ 同一套身份：企微 JWT  或  sk-xxxx
   ▼
统一平台（src/ 一个 repo）
   ├─ auth/     认证 + RBAC + 数据权限           （地基，M1）
   ├─ admin/    管理后台 API（用户/角色/部门/号池/模型/配额）  （M1 部分 + M3）
   ├─ gateway/  LLM 网关：三协议原生直通 + litellm.Router  （M2，✅ 完成）
   ├─ mcp/      标准 MCP 服务器（FastMCP，零新表）        （M4，未动工）
   └─ tasks/    ARQ 后台任务（用量汇总/配额重置/清理）     （M5，骨架存在）
   共享：PostgreSQL + Redis
```

身份模型（架构稿 G5）：**主体落库统一是 `sys_user.id`（代码/API 命名 `user_id`）**；凭据分层：管理后台与资源管理 API 只接受平台 JWT/企微 JWT；LLM 推理协议端点与 MCP 协议端点额外接受 `sk-key` 作为程序调用凭据。二者最终都归因到同一个 `user_id` 与配额桶，但 `sk-key` 不是后台登录态。

---

## 一、里程碑总览

| 里程碑 | 主题 | 真实状态 | 一句话范围 |
|---|---|---|---|
| **M0** | 项目基座 | ✅ 完成 | 可启动 + 连库 + 16 表迁移 + 统一信封/异常 + i18n 骨架 + structlog + Redis 客户端 + 真实就绪探针 |
| **M1** | 用户 / 凭证 / RBAC | ✅ **完成** | 账密登录 + argon2 + RS256 自签 JWT + sk-key（仅 LLM/MCP 协议调用端点）+ 统一鉴权拦截 + 开放白名单 + 接口权限 + 数据权限（user + role 域）+ `create_dept` + 当前用户资料/改密 + 用户/角色/部门/菜单 CRUD + 动态菜单 + **完整会话生命周期（logout/refresh 轮换/jti 令牌吊销/在线用户/登录锁定 + IP 限流/密码强度 + 强制登出）** |
| **M2** | LLM 网关（热路径） | ✅ **完成** | 三协议原生直通入口 + 模型路由 + litellm.Router 韧性 + Lua 原子配额 + 用量记账 |
| **M3** | 管理后台 API | 🟡 部分完成 | 用户/角色/部门/菜单 CRUD 已随 M1 落地；users/roles 统一分页 + 统一查询过滤/排序/批量/脱敏基建 + 审计日志（操作+登录）+ 在线用户 + 平台配置 + 管理员代重置密码 + OSS 附件基建（私有桶上传/头像绑定）已落地；剩 credentials/catalog/grants/usage/quota |
| **M4** | MCP 服务器 | ⬜ 仅骨架 | `src/mcp/server.py` 存在但未接 FastMCP 能力面 |
| **M5** | ARQ 后台任务 | ⬜ 仅骨架 | `tasks/{worker,schedule,registry}.py` 存在；领域任务未写 |
| **M6** | 企微对接 | ⬜ 未动工 | 企微 OAuth / JWT 验签作为第二凭证入口 |

**排序铁律**（违反会返工）：
- **M1 先于一切业务面**：网关与后台都依赖鉴权地基；先做地基，否则各自重造一套鉴权。
- **M2 配额原语就地建**：Redis 配额计数/失效原语随 M2 消费者落地，不在 M0 提前堆砌。
- **M3 依赖 M1 的 RBAC**：后台 CRUD 的权限门禁必须建立在 M1 判定之上。
- **M4 晚于 M2/M3**：MCP 经服务层暴露能力，须等服务边界稳定。
- **M6 最后做**：企微是外部适配器，用户/RBAC 用平台自签凭证已可独立闭环、完全可测。

---

## 二、后台管理系统基本盘清单（直面「是否具备后台基本要求」）

> 以成熟企业级后台（若依 RuoYi 等）为标尺，逐项核对**当前代码**是否具备。这是衡量「能不能算一个后台」的硬指标。

| 分类 | 能力项 | 状态 | 说明 / 缺口 |
|---|---|---|---|
| 登录会话 | 账密登录 | ✅ | `POST /auth/login`，argon2 校验，发 RS256 自签 JWT |
| 登录会话 | 当前用户信息 | ✅ | `GET /auth/me`，返回 `user_id`、基础资料、语言偏好 + 权限码集合；管理面 JWT-only |
| 登录会话 | 退出登录（登出） | ✅ | `POST /auth/logout` 吊销当前会话；基于 jti 的会话白名单 + Redis 吊销（`src/core/session_store.py`） |
| 登录会话 | 刷新令牌（refresh） | ✅ | `POST /auth/refresh` 不透明 refresh（sha256）+ 轮换 + 重用检测；旧 token 吊销 |
| 登录会话 | 修改密码 / 重置密码 | ✅ | `PUT /auth/me/password` 自助改密（校验旧密码 + 强度策略，改密后强制登出全部会话）；`POST /admin/users/{id}/reset-password` 管理员代重置（可见性门禁 + 强度校验 + 吊销目标全部会话，perm `system:user:resetPwd`）；缺用户自助忘记密码流程（无邮箱/短信通道，按需） |
| 登录会话 | 个人中心 | 🟡 部分 | `PUT /auth/me` 已支持姓名/邮箱/手机/语言偏好自助更新 + 头像绑定（`avatar` 存附件 id，绑定校验所有权 + biz_type）；`GET /auth/me` 返回预签名 `avatar_url`；缺个人 sk-key 自助管理入口（列表/创建/吊销），留 M3 credentials 模块统一落地 |
| 登录会话 | SSO / 企微登录 | ⬜ 未做 | `UserOAuth`/`OAuthSource.wecom` 模型存在；无 OAuth 回调、JWT 验签、账号绑定流程 |
| 登录会话 | 会话安全策略 | ✅ | token jti + Redis 会话白名单 + 吊销；refresh 轮换 + 重用检测；登录失败按用户锁定（5 次/15 分钟）+ 按 IP 滑窗限流；密码强度策略；改密强制登出全部会话。缺验证码/MFA（按需） |
| 鉴权边界 | 统一登录拦截 | ✅ | `AuthMiddleware` 默认拦截非公开路径，解析用户写入 `request.state.user` |
| 鉴权边界 | 开放接口白名单 | ✅ | `PUBLIC_PATHS` 显式列出 `/auth/login`、health、OpenAPI/docs/redoc |
| 鉴权边界 | sk-key 使用边界 | ✅ | 仅 LLM 推理协议端点（OpenAI/Anthropic/Gemini）与 MCP 单协议端点允许；后台与资源管理 CRUD 均 JWT-only |
| 权限模型 | 接口权限（perms 码） | ✅ | 已给 user/role/dept/menu + 审计/在线/配置等 admin 端点挂 `RequiredPerms` |
| 权限模型 | 超级管理员 bypass | ✅ | 持有 `superadmin` role code 短路接口权限 + 数据权限两条线（bootstrap-safe，非 `*:*:*` 通配，见 §4.6） |
| 权限模型 | 数据权限 data_scope 6 档 | 🟡 部分 | 判定全实现，已应用到 user 域与 role 域；新业务域待随 M3 落地 |
| 权限模型 | 角色授权防提权 | ✅ | 用户角色分配会校验 actor 可授出的权限集合；超管短路 |
| 权限模型 | 菜单/权限管理 | ✅ | `sys_menu` CRUD、按钮权限维护、当前用户动态菜单接口已落地；管理端点挂 `system:menu:*`，动态菜单只要求 JWT |
| 组织用户 | 用户管理 CRUD | ✅ | `src/admin/users/`，含创建/更新/删除/角色分配、越权移动守卫 |
| 组织用户 | 角色管理 CRUD | ✅ | `src/admin/roles/`，含 RoleMenu/RoleDept 链管理、code 唯一 |
| 组织用户 | 部门管理 CRUD | ✅ | `src/admin/departments/`，含环路守卫 + 删除阻断 |
| 组织用户 | 岗位/职级/用户组 | ❌ 缺 | 成熟后台常见组织维度未建模；当前只做部门 + 角色 |
| 组织用户 | 批量导入/导出用户 | ❌ 缺 | 缺 Excel/CSV 导入导出、模板下载、导入错误报告 |
| 列表体验 | 统一分页响应 | ✅ | `SuccessEnvelope[Page[T]]` 已用于 users/roles/审计/配置全部列表端点；部门/菜单因树结构按设计返回 `list`（无分页语义），非缺口 |
| 列表体验 | 查询过滤 / 排序 | ✅ | `src/core/query.py` 统一 `ListQuery` + `resolve_sort`（白名单列防注入）；users/roles/审计/配置列表已接入关键词检索 + 排序 |
| 列表体验 | 批量操作 | ✅ | `BatchIdsRequest`/`BatchResult` 通用批量 DTO + users/roles/departments/menus 四大模块均已落地 `POST /batch-delete`；批量启停/批量分配等随 M3 业务端点扩展 |
| 列表体验 | 字段脱敏 / 敏感字段隐藏 | ✅ | `src/core/query.py` `mask_fields` 统一脱敏（非超管掩码手机/邮箱，超管原值）；password 不出 Read |
| 审计日志 | 操作日志 | ✅ | `oper_log` 表 + 审计中间件留痕（谁/何时/对什么/前后值/trace_id/IP/耗时）；`GET /admin/audit/oper-logs` 分页查询，perm 门禁 |
| 审计日志 | 登录日志 | ✅ | `login_log` 表记录登录成功/失败 + 用户名 + IP + UA + 失败原因；`GET /admin/audit/login-logs` 分页查询，perm 门禁 |
| 审计日志 | 在线用户 / 令牌管理 | ✅ | 基于 Redis 会话白名单：`GET /admin/online/sessions` 查在线会话、`DELETE /admin/online/sessions/{jti}` 踢人；perm 门禁 `system:online:*` |
| 平台配置 | 参数配置 | ✅ | `sys_config` 键值表（typed：string/int/bool/json）+ `/admin/config` CRUD（perm `system:config:*`，内置项可改不可删）+ `src/core/config_accessor.py` 类型化短 TTL 缓存读取 |
| 平台配置 | 数据字典 | ✅ 决策不做 | 走 i18n code + 枚举 codegen 路线，刻意否决 DB 字典表，非缺口 |
| 平台配置 | 国际化资源管理 | ✅ | 后端 locale 中间件（`LocaleMiddleware`）+ ContextVar 解析链已落地；按架构设计「后端只发 code，前端拥有展示文本」，ErrorCode 枚举 21 个稳定 code 供前端 i18n key 使用，后端无需翻译资源文件；Pydantic 422 msg i18n 为低优方向待定项，不阻塞业务 |
| 平台资源 | sk-key 管理后台 | ⬜ 未做 | `api_key` 表与解析已就绪；缺创建/吊销/列表/权限边界/一次性明文返回端点 |
| 平台资源 | 模型目录 / 渠道 / 号池 | ⬜ 未做 | 表已建；缺 catalog/channel/channel_key/deployment 管理 API |
| 平台资源 | 模型分配 | ⬜ 未做 | `user_model_grant` 表已建；缺用户/部门授权、默认模型、即时生效机制 |
| 平台资源 | 配额配置 / 用量报表 | ⬜ 未做 | quota/usage 表已建；缺 Redis 扣减原语、读侧报表、导出、周期重置 |
| 运维可观测 | 健康探针 | ✅ | `/health/live` 裸活 + `/health/ready` 探测 PG/Redis |
| 运维可观测 | 结构化日志 / trace_id | ✅ | `TraceIdMiddleware` + structlog contextvars；错误信封带 trace_id |
| 运维可观测 | 指标监控 | ❌ 缺（刻意推迟） | 无 Prometheus/OpenTelemetry 指标、慢查询/接口耗时、告警。**刻意推迟**：属横切观测基建，依「第一个真实消费者出现再引入」原则，随 M2 网关热路径上量时统一接入（届时指标才有意义）；不阻塞业务开工，详见 §六推迟项 |
| 运维可观测 | 后台任务管理 | ⬜ 未做 | ARQ 骨架存在（已接对专用 Redis 库 db 1）；缺任务列表、重试、调度查看、失败告警，随 M5 落地 |
| 安全加固 | 上传体积/解压炸弹防护 | ✅ | 上传分块限额读取（超 `max_bytes` 即拒，不全量入内存）+ 头像 `DecompressionBombError` 防护；生产配 nginx `client_max_body_size` 边缘第一道闸 |
| 安全加固 | 密码强度全路径一致 | ✅ | `create_user`/`update_user`/`reset_password`/自助改密四路径统一过 `password_strength_violations`，堵管理员弱口令 |
| 安全加固 | 生产配置 fail-fast | ✅ | `config.validate_runtime`：非 local 缺 JWT key / 开 debug / CORS 通配 / 信任代理跳数<1 即拒启动 |
| 安全加固 | 安全响应头 | ✅ | `SecurityHeadersMiddleware`：nosniff / X-Frame-Options DENY / CSP `default-src 'none'` / 条件 HSTS（HTTPS 才发）；docs 页豁免 CSP |
| 安全加固 | 附件 stored-XSS 防护 | ✅ | 通用附件预签名强制 `Content-Disposition: attachment`（签名覆盖响应头，客户端不可篡改）；头像内联展示不受影响 |
| 安全加固 | 全局限流（登录外） | ❌ 缺（边缘层职责） | 登录维度限流已有（按用户名锁定 + 按 IP 滑窗）；通用接口限流**刻意留在 nginx 边缘层**（架构定位反代职责），不在应用层重造，详见 §六推迟项 |

**结论**：当前已具备后台地基、用户/角色/部门/菜单四大基础 CRUD、动态菜单、当前用户资料/改密/头像、RBAC、统一分页/查询过滤/排序/批量/脱敏基建（全部列表端点已接入，树结构按设计不分页）、完整登录会话生命周期（登出/刷新/吊销/在线用户/锁定限流/密码强度）、操作与登录审计日志、平台参数配置、国际化 code 路线（后端只发 code + locale 中间件）、管理员代重置密码、OSS 私有桶附件基建（统一上传 + 头像自助绑定 + 短期预签名读取），并经一轮安全/性能加固（上传限额、解压炸弹防护、密码强度全路径一致、生产配置 fail-fast、安全响应头、附件 stored-XSS 防护、ARQ Redis 库隔离）。测试覆盖率 **98%**（693 passed）。它已是一个**功能完整、达到生产开工标准的企业级后台地基**，可安全支撑后续业务模块。剩余主要是 AI 平台资源管理（sk-key 含个人密钥入口/模型目录/号池/模型分配/配额用量）与运维可观测（指标监控/任务管理），随 M2/M3 业务面继续补完——其中指标监控与通用限流是**刻意推迟**的横切/边缘层基建（见 §六），非地基缺口，不阻塞业务开工。

---

## 三、数据模型现状与漂移（对齐数据模型设计 v0.5.5）

### 3.1 已落地 20 张表

`src/db/models/` 按领域拆分，全部继承三类基类之一：

| 文件 | 模型（类名 → 表名） |
|---|---|
| `identity.py` | User→`sys_user`、Department→`sys_department`、UserOAuth→`sys_user_oauth`、Role→`sys_role`、Menu→`sys_menu`、UserRole→`sys_user_role`、RoleMenu→`sys_role_menu`、RoleDept→`sys_role_dept` |
| `credential.py` | ApiKey→`api_key` |
| `model_catalog.py` | UpstreamChannel→`upstream_channel`、ChannelKey→`channel_key`、LogicalModel→`logical_model`、ModelDeployment→`model_deployment` |
| `grant.py` | UserModelGrant→`user_model_grant` |
| `usage.py` | UsageRecord→`usage_record`（`LogEntity`） |
| `quota.py` | Quota→`quota` |
| `audit.py` | OperLog→`oper_log`（`LogEntity`）、LoginLog→`login_log`（`LogEntity`） |
| `sys_config.py` | SysConfig→`sys_config` |
| `attach.py` | SysAttach→`sys_attach`（`BaseEntity`；object_key/bucket/original_name/content_type/file_size/biz_type） |

三类基类（[`src/db/base.py`](../src/db/base.py)）：
- `BaseEntity`：`id / is_deleted / created_by / create_dept / created_at / updated_by / updated_at`（14 张配置/业务表继承，含 `sys_config`、`sys_attach`）
- `LogEntity`：`id / created_at`（append-only 流水：UsageRecord / OperLog / LoginLog）
- `LinkEntity`：`id / created_at`（关联表，物理删）

**迁移现状**：迁移头 `e2bec51025e0`（`sys_attach` 表 + `sys_user.avatar` 列，单一 head）；链路 `2052dfbbf16e`（首迁移 16 表）→ `dd58396c39de`（oper_log + login_log）→ `86f8cd47f40f`（sys_config）→ `e2bec51025e0`（sys_attach + avatar）。前三段历史迁移已验证 up/down 可逆；`e2bec51025e0` 已离线核实上/下行 SQL 对称生成（建表+3 索引+加列 ↔ 反序删列+3 索引+删表）。

### 3.2 ✅ `BaseEntity.create_dept` 已补齐（部门级数据权限硬前提）

**现状**：`BaseEntity` 已新增 `create_dept`（普通索引），首迁移同步覆盖所有配置/业务表。`UserService`、`RoleService`、`DepartmentService` 创建数据时从当前用户部门写入。

**语义决策**：
1. user 域仍按 `User.department_id` 过滤，因为用户管理屏关注「目标用户属于哪个部门」。
2. role 域与后续通用业务表按 `create_dept` + `created_by` 过滤，因为它们是管理资源，部门归属应取创建者当时所在部门。
3. `LogEntity` 与 `LinkEntity` 不加 `create_dept`：流水 append-only，关联表是纯链接。

### 3.3 ✅ 表名边界：identity/RBAC 用 `sys_`，业务表不用

当前代码与数据模型文档一致：身份/RBAC 基础表使用 `sys_` 前缀（`sys_user` / `sys_role` 等），AI 网关业务表保持短表名（`api_key` / `usage_record` / `quota` / `upstream_channel` 等）。

---

## 四、鉴权与权限体系现状（全链路核实）

> 这是用户重点追问的部分：登录接口、统一拦截、开放白名单、接口权限、数据权限、超管 bypass。逐条给代码现状 + 缺口。

### 4.1 登录接口

**✅ 已落地**（[`src/auth/router.py`](../src/auth/router.py)）：

| 端点 | 方法 | 说明 |
|---|---|---|
| `/auth/login` | POST | 账密 → RS256 自签 access token + 不透明 refresh token；登录失败按用户锁定 + 按 IP 限流 |
| `/auth/logout` | POST | 吊销当前会话（jti 从白名单移除） |
| `/auth/refresh` | POST | 不透明 refresh（sha256）→ 轮换发新 access+refresh，旧 token 吊销 + 重用检测 |
| `/auth/me` | GET | 当前用户 + 权限码集合 + 预签名 `avatar_url`（需 JWT；返回 `user_id`） |
| `/auth/me` | PUT | 当前用户更新姓名/邮箱/手机/语言偏好/头像绑定（需 JWT） |
| `/auth/me/password` | PUT | 当前用户校验旧密码 + 强度策略后改密，改密后强制登出全部会话（需 JWT） |

**服务层解析器**（无 HTTP 面，[`src/auth/service.py`](../src/auth/service.py)）：
`resolve_access_token`（JWT 验签 + 会话白名单校验）、`resolve_api_key`（sk-key → sha256 → 查 `api_key`）、`authenticate_password`（throttle 门禁 + 发 JWT + 建会话）、`refresh_session`（轮换）、`logout`（吊销）、`change_current_password`（强度校验 + 强制登出）。会话态在 Redis（[`src/core/session_store.py`](../src/core/session_store.py)），登录限流在 [`src/core/login_throttle.py`](../src/core/login_throttle.py)。

**⚠️ 缺口**：用户自助忘记密码/重置流程（管理员代重置已落地，缺无人值守邮箱/短信通道）、个人 sk-key 入口等完整个人中心（头像已落地）、企微回调（`OAuthSource.wecom` 枚举 + `UserOAuth` 模型存在，但无端点无逻辑，留 M6）。

### 4.2 ✅ 统一登录拦截：已落地

[`src/main.py`](../src/main.py) 已挂 `AuthMiddleware`（位于 trace-id 之后的请求链路内，401 信封带 trace_id）。
中间件对所有非公开路径默认要求有效凭据，解析后写 `request.state.user`。

凭据边界：
- 管理后台、`/auth/me`、资源管理类 CRUD：只接受 JWT，走 `get_current_jwt_user` / `CurrentJwtUser`。
- LLM 推理协议端点：允许 JWT 或 sk-key，当前白名单为 `POST /v1/chat/completions`、`POST /v1/messages`、`POST /v1beta/models/{model}:generateContent`、`POST /v1beta/models/{model}:streamGenerateContent`。
- MCP 协议端点：允许 JWT 或 sk-key，当前只放开单端点 `/mcp`（GET/POST）；不放开 `/mcp/*` 资源/管理子路径。
- RBAC 仍由 `RequiredPerms` 处理，统一拦截只负责“有没有合法用户”和“凭据类型是否允许”。

### 4.3 ✅ 开放接口白名单：已落地

[`src/auth/middleware.py`](../src/auth/middleware.py) 定义中央 `PUBLIC_PATHS`：
`/auth/login`、`/auth/refresh`、`/health/live`、`/health/ready`、`/docs`、`/docs/oauth2-redirect`、`/openapi.json`、`/redoc`。
（`/auth/refresh` 公开是因为它不携带 access token——身份由不透明 refresh token 自证 + Redis 会话查表，故不能要求先过 access 鉴权。）白名单支持 `api_prefix` 归一化，避免部署到 `/api` 后误拦截。

### 4.4 ✅ 接口权限（perms 码）：admin 域完善

`RequiredPerms("域:资源:动作")` 依赖工厂（[`src/auth/dependencies.py`](../src/auth/dependencies.py)），挂在全部 admin 端点：

```
users:   system:user:{list,add,edit,remove,resetPwd}
roles:   system:role:{list,add,edit,remove}
depts:   system:dept:{list,add,edit,remove}
menus:   system:menu:{list,add,edit,remove}
audit:   system:operlog:list / system:loginlog:list
online:  system:online:{list,kick}
config:  system:config:{list,query,add,edit,remove}
```

判定 `AuthenticatedUser.has_permission()` = `is_superuser or required in permissions`。权限不入 JWT，每请求从 DB 聚合（`User→UserRole→Role→RoleMenu→Menu`，[`src/auth/repository.py`](../src/auth/repository.py)）。
**额外守卫**：`_require_assignable_roles` 防提权（授出的权限须为 actor 权限子集）。

### 4.5 🟡 数据权限（data_scope 6 档）：判定全实现，已应用 user + role 域

**✅ 判定全实现**（[`src/auth/service.py`](../src/auth/service.py) `resolve_data_scope`）：`all / custom / dept / dept_and_child / self / dept_and_child_or_self` 六档齐全；多角色取并集、最宽优先；`dept_and_child` 走邻接表子树遍历（`_collect_subtree`）；部门树缓存 + 变更失效（[`src/auth/dept_tree_cache.py`](../src/auth/dept_tree_cache.py)）；无部门时 fail-closed。

**✅ 已应用**：
- user 域：[`src/admin/users/repository.py`](../src/admin/users/repository.py) 按 `User.department_id` + `User.id` 翻成 SQL WHERE，并用于列表 + 单条 mutation 可见性守卫。
- role 域：[`src/admin/roles/repository.py`](../src/admin/roles/repository.py) 按 `Role.create_dept` + `Role.created_by` 翻成 SQL WHERE，并用于列表 + 单条 mutation 可见性守卫。

**⚠️ 缺口**：
- **department 列表不过滤**（树导航的结构性需要，待定）。
- **其他域全无**：api_key / grant / quota / model_catalog / channel。
- **M3 新业务域要沿用 `create_dept`/`created_by` 通用规则**，禁止再按端点临时拼权限。

### 4.6 ✅ 超级管理员 bypass：两条线均覆盖

超管判定 = **持有 `code == "superadmin"` 的在用角色**（`AuthenticatedUser.is_superuser` → `SUPERADMIN_ROLE_CODE in role_codes`，常量在 [`src/auth/constants.py`](../src/auth/constants.py)）。验证覆盖：
- **接口权限**：`has_permission` → `is_superuser` 短路；`RequiredPerms` 依赖随之放行。
- **数据权限**：`resolve_data_scope` 检测 `is_superuser` → 直接返回 `unrestricted=True`。
- **提权守卫**：`UserService` 中 `actor.is_superuser` 跳过 `_require_assignable_roles`；且把 `superadmin` 角色本身列为非超管不可授出（防经角色分配间接提权）。

**说明**：超管由**角色身份**（`superadmin` role code）承载，而非某个魔法权限字符串——bootstrap-safe（不依赖 menu/role_menu 链，种子管理员建出来即生效）。早期文档曾写「靠 `*:*:*` 通配权限」，与代码不符，已据实更正。`has_permission` 仍支持精确权限码匹配，但「全局放行」走的是 role code 这条线，不是通配权限。

---

## 五、各里程碑详细分解

### M0：项目基座 ✅

**📁 已落地**：
- **工程骨架**：`pyproject.toml`（fastapi / sqlalchemy[asyncio] / asyncpg / pydantic / alembic / arq / redis / httpx / litellm / mcp / pyjwt[crypto] / cryptography / uvicorn / structlog / argon2-cffi，全带版本上界）；`ruff`（line 100，E/F/I/B/UP/SIM）+ `mypy --strict`。
- **数据层**：[`src/db/base.py`](../src/db/base.py)（三基类）、[`src/db/session.py`](../src/db/session.py)（async engine + `get_session`）、[`src/db/repository.py`](../src/db/repository.py)（`BaseRepository[Model]` 泛型 CRUD）、`src/db/models/`（16 表）、[`src/core/snowflake.py`](../src/core/snowflake.py)（雪花 ID，开发 worker-id 固定 0，含时钟回拨 spin）、`alembic/` 首迁移可跑。
- **横切**：[`src/config.py`](../src/config.py)（pydantic-settings）、[`src/enums.py`](../src/enums.py)（集中 StrEnum + ErrorCode，成员强制行内注释）、[`src/responses.py`](../src/responses.py)（统一信封）、[`src/exceptions.py`](../src/exceptions.py)（`AppError` + 统一错误信封拦截器，方案 B 扁平同构，沿用 RFC 9457 的 code+params 理念但不用 problem+json media type）、`src/core/i18n/`（locale ContextVar + 解析中间件）、[`src/core/logging.py`](../src/core/logging.py)（structlog + trace_id 自动注入）、[`src/core/redis.py`](../src/core/redis.py)（最小异步客户端，仅生命周期 + 健康探测）、[`src/main.py`](../src/main.py)（app 工厂 + lifespan + `/health/live` 裸活 + `/health/ready` 真实探测 PG+Redis）。

**⚠️ 缺口/遗留**：Pydantic 422 校验错误 `errors[].msg` 仍发英文原文（i18n 方向待定）。

### M1：用户 / 凭证 / RBAC ✅

**📁 已落地**：
- **核心安全层** [`src/core/security.py`](../src/core/security.py)：argon2 密码哈希（verify 不匹配/畸形均折叠 False）、sk-key sha256 校验、平台自签 JWT（**硬锁 RS256**，decode 显式传 `algorithms=["RS256"]` 防算法混淆，claims `extra="forbid"`，最小集 `sub/iat/exp/token_use` + `jti`，权限不入 JWT，`sub=sys_user.id`）；access/refresh 签发 + refresh sha256 哈希；密码强度策略 `password_strength_violations`（长度 + 必含字母与数字）。
- **会话安全层** [`src/core/session_store.py`](../src/core/session_store.py)：Redis 会话白名单（allowlist，按 jti），不透明 refresh 轮换（GETDEL 原子消费 + 重用检测）、在线会话枚举（全局索引 `sess:users`，存 ip/ua/login_at）、单会话吊销 + 全量强制登出；[`src/core/login_throttle.py`](../src/core/login_throttle.py)：登录失败按用户锁定（5 次 / 15 分钟）+ 按 IP 滑动窗口限流（门禁前置于 DB 查询 + argon2，省 CPU + 防枚举）。
- **auth 域** `src/auth/`：login/logout/refresh/me 端点；`AuthRepository`（权限码聚合）；`AuthService`（账密校验 + throttle + 建会话 / token 与 key 解析 + 白名单校验 / RBAC 判定 / 数据范围解析 / refresh 轮换 / logout / 改密强制登出，frozen DTO `AuthenticatedUser` + `DataScopeFilter`）；`credentials.py`（统一 HTTP 凭据识别）；`middleware.py`（`AuthMiddleware` + `PUBLIC_PATHS` + `sk-key` 协议端点白名单）；`dependencies.py`（`CurrentUser` 给 LLM/MCP 协议处理器，`CurrentJwtUser` 给管理面，`TraceId` / `RequiredPerms`）；认证失败折叠不透明码（坏凭据/坏 token/禁用用户/会话已吊销 → 401，越权 → `auth.forbidden` 403，账户锁定 → 429）。
- **当前用户资料/改密** `src/auth/`：`GET /auth/me` 返回用户资料 + 权限码；`PUT /auth/me` 允许当前用户维护姓名/邮箱/手机/语言偏好；`PUT /auth/me/password` 强度校验 + 校验旧密码后写入 argon2 新哈希 + 吊销全部会话。三者均 JWT-only。
- **用户/角色/部门/菜单 CRUD** `src/admin/`（提前完成 M3 一部分）：departments（环路守卫 + 删除阻断）；menus（Menu/按钮权限 CRUD + 当前用户动态菜单 + 删除阻断）；roles（RoleMenu/RoleDept 链 + code 唯一 + 删除清 UserRole）；users（UserRole 分配 + argon2 + 3 道安全守卫：越权创建/移动防护、角色分配提权防护、工号唯一）。挂 `/admin`，perm 门禁 `system:{dept,menu,role,user}:*`。
- **数据权限基础**：`BaseEntity.create_dept` 已补；user 域按 `department_id`，role 域按 `create_dept/created_by`。
- **凭据边界**：sk-key 只允许 LLM 推理协议端点与 MCP 单协议端点，不再等同后台 JWT；后台管理面与资源管理 CRUD 均 JWT-only。

**⚠️ 缺口**（不阻塞 M1，归后续/M6）：
1. 用户自助忘记密码/重置流程（管理员代重置已落地，头像已落地，剩个人密钥入口）（后续）。
2. 企微 OAuth 回调（留 M6）。
3. M3 业务域数据权限待随端点落地（credentials/catalog/grants/usage/quota 沿用 `create_dept/created_by` 通用规则）。

**✅ 质量门**：当前测试套件 **693 passed / 2 skipped**（覆盖率 98%），`mypy --strict`（`mypy src`）0 error、ruff check clean。

### M2：LLM 网关（热路径）✅ 完成

**📁 已落地**：

- **三协议原生直通入口**（[`src/gateway/router.py`](../src/gateway/router.py)）：
  - **OpenAI** `POST /v1/chat/completions` → `Router.acompletion(stream=True)` → OpenAI SSE chunks 直透
  - **Anthropic** `POST /v1/messages` → `Router.aanthropic_messages(stream=True)` → 原生 Anthropic SSE bytes 直透（零格式转换）
  - **Gemini** `POST /v1beta/models/{model}:generateContent` / `:streamGenerateContent` → `Router.agenerate_content_stream()` / `Router.agenerate_content()` → 原生 Gemini SSE bytes 直透
  - **关键设计**：Anthropic/Gemini 走 litellm Router 原生 provider 接口，返回 `AsyncIterator[bytes]`，网关直管道输出，**无 double-normalization**（不经 OpenAI 中间格式再反转）。
- **模型路由 + 鉴权**（[`src/gateway/service.py`](../src/gateway/service.py)）：`resolve_model` 按用户授权裁决逻辑模型 → litellm Router 按 deployment 权重/优先级路由到具体上游渠道。
- **litellm.Router 装配**（[`src/gateway/router_factory.py`](../src/gateway/router_factory.py)）：DB（逻辑模型 → 承载渠道 → 号池 key）笛卡尔展开为 Router deployment 列表；channel_key Fernet 解密仅在构建时；channel_id 写入 `model_info.id` 供记账归因。
- **Router 单例管理**（[`src/gateway/router_manager.py`](../src/gateway/router_manager.py)）：poll + Redis pub/sub 双通道热更新；渠道/号池变更 → 1s 内 Router 重建生效。
- **Lua 原子配额**（[`src/gateway/quota.py`](../src/gateway/quota.py)）：Redis Lua 脚本 INCR+EXPIRE 原子操作；pre-flight reservation + post-flight settlement；cost 配额用 micro-units（×1_000_000）避免浮点精度问题。
- **用量记账**（[`src/gateway/usage.py`](../src/gateway/usage.py)）：fire-and-forget `asyncio.create_task` 写 `usage_record`（独立 DB session，不阻塞响应流）。
- **Channel ID 归因机制**：传 `litellm_metadata={}` mutable dict 给 Router，Router 内部 `_update_kwargs_with_deployment()` 写入 `model_info.id`（即 channel_id）——确定性，无回调依赖。
- **Usage 提取**：从原生 SSE bytes 中间流解析（Anthropic: `message_start`/`message_delta` 事件；Gemini: `usageMetadata` 字段），不依赖 litellm callbacks（已知 bugs #8842/#24097/#28216）。
- **请求 schemas**（[`src/gateway/schemas.py`](../src/gateway/schemas.py)）：`OpenAIRequest`、`AnthropicRequest`（`max_tokens: int = 4096`）、`GeminiRequest`（`generationConfig: dict | None`）。

**⚠️ 缺口**（不阻塞 M2 核心，归后续）：
1. M3 管理面落地后，模型目录/渠道/号池 CRUD 才能通过 UI 配置（当前 Router 配置需直接操作 DB）。
2. 指标监控（Prometheus/OTel）随上量统一接入（见 §六 G17）。
3. litellm passthrough 路径异常类型 bug（#20507）的外层兜底分类（当前依赖 Router 内置重试）。

**✅ 质量门**：当前测试套件 **772 passed / 2 skipped**（含 20 个 streaming 专项测试），ruff check clean。

**依赖**：M1 的鉴权依赖 + sk-key 解析；sk-key 只用于推理协议入口，模型/渠道/号池等管理 API 必须 JWT-only。

### M3：管理后台 API 🟡 部分完成

**✅ 已随 M1/Wave A-D 完成**：users/roles/departments/menus CRUD（含守卫 + data_scope）、当前用户资料/改密、当前用户动态菜单、users/roles 的 `SuccessEnvelope[Page[T]]` 分页；统一查询过滤/排序（`src/core/query.py` `ListQuery`+`resolve_sort`）、批量操作 DTO（`BatchIdsRequest`/`BatchResult`）、字段脱敏（`mask_fields`）；审计日志（操作 + 登录，`/admin/audit/*`）、在线用户/踢人（`/admin/online/*`）、平台参数配置（`/admin/config/*` + `src/core/config_accessor.py`）；**管理员代重置密码（`POST /admin/users/{id}/reset-password`，可见性门禁 + 强度校验 + 吊销目标全部会话）**；**OSS 附件基建（私有桶对象存储封装 + 统一上传接口 + 用户头像自助绑定，见下「OSS/附件」段）**。
**⚠️ 待做**：credentials（api_key 管理）、catalog（逻辑模型/渠道/号池）、grants（模型分配）、usage（用量读侧）、quota（配额配置）；敏感字段加解密落各 service（`channel_key.api_key_encrypted` Fernet，届时补回 config 的 fernet_key）。

### M3 附属：OSS / 附件基建 ✅

**📁 已落地**：
- **对象存储封装** [`src/core/oss.py`](../src/core/oss.py)：`ObjectStorage`（aiobotocore，S3 兼容，私有桶）；path-style 寻址（裸 IP endpoint 无法走 virtual-host）+ `before-send.s3` 钩子剥 `Expect: 100-continue` 头（否则写请求对 MinIO 无限挂起）；put/get/delete + 短期预签名 URL（TTL 走 config）。配置走 `.env`（endpoint/bucket/access-key/secret-key），未配置时 `OptionalObjectStorageDep` 优雅降级为 `None`。
- **图片处理** [`src/core/image.py`](../src/core/image.py)：头像 webp 转码 + 剥 EXIF；core 层 web-agnostic，异常不继承 `AppError`（翻译留服务层）。
- **统一上传接口** `src/files/`：`POST /attach/upload` 按 `biz_type` 分发——`avatar` 走 webp 转码（≤2MB），其余 `attachment` 原样存储保留类型/扩展名（≤10MB）；`_safe_extension` 阻断路径穿越；object-first 落盘后写 `sys_attach` 元数据；`AttachRead.from_attach` 现算预签名 url。（包名 `files/`=业务域，对标 fullstackhero `Modules/Files`；路由/表/类名仍用 `attach`，仅包路径改名。）
- **头像自助绑定** `src/auth/`：`PUT /auth/me` 绑定 `avatar`（附件 id，校验所有权 + biz_type，非本人/类型错 → 404）；`GET /auth/me` 返回预签名 `avatar_url`（OSS 未配置时为 None）。

**✅ 质量门 + 真实验证**：附件 service 9 测 + e2e 5 测 + auth 头像 8 测 + 管理员重置 7 测全绿；**真实 MinIO（150.158.129.101:9000）端到端 QA 通过**：upload→presign→HTTP GET 字节级一致→delete→404。

**⚠️ 缺口**：附件软删未联动 OSS 物理清理（DB 软删，对象留存，待 M5 清理任务）；附件下载端点（当前只预签名读，无统一下载代理）；按 biz_type 的更细粒度配额/类型白名单（当前只 size-gate）。

### M4：MCP 服务器 ⬜ 仅骨架

`src/mcp/server.py` 存在但未接 FastMCP 三 primitive。范围：FastMCP（`stateless_http=True`）+ `TokenVerifier`（复用 user_id/key 鉴权，仅 MCP 协议端点接受 sk-key）+ 工具实现（平台内代码，内部调第三方 REST）。**零新表**，不绕过 RBAC/配额。依赖 M2/M3 服务边界稳定。

### M5：ARQ 后台任务 ⬜ 仅骨架

`tasks/{worker,schedule,registry}.py` 存在（registry 已承载 `noop` 以断开 cron eager-import 循环），领域任务未写。范围：夜间用量汇总、周期配额重置、账单滚动、清理。**scheduler 单实例铁律**，任务幂等。依赖 M2/M3 用量/配额模型稳定。

### M6：企微对接 ⬜ 未动工

企微 JWT 验签（无状态，公钥/JWKS）+ OAuth 登录流（命中 `sys_user_oauth` 硬匹配 → 平台用户）+ refresh token。**前置**：企微 JWT claims 契约待明确。依赖 M1 用户核心（企微只是额外凭证入口）。

---

## 六、已识别缺口与待办登记册（跨里程碑）

> 集中登记本次梳理暴露的全部缺口，按优先级排。每条标注归属里程碑 + 阻塞关系。

| # | 缺口 | 归属 | 优先级 | 备注 |
|---|---|---|---|---|
| G1 | `BaseEntity` 补 `create_dept` + insert 时写入 | ✅ 已完成 | - | 已落 `BaseEntity`、首迁移、user/role/dept 创建写入 |
| G2 | 数据权限泛化到其他业务域（按 `create_dept`/`created_by` 过滤） | M3 随业务端点 | **高** | role 域已完成；credentials/catalog/grants/usage/quota 待随端点落地 |
| G3 | 统一鉴权拦截 `AuthMiddleware` + `request.state.user` | ✅ 已完成 | - | 管理/资源 API JWT-only；LLM/MCP 协议端点可用 sk-key |
| G4 | 中央 `PUBLIC_PATHS` 开放接口白名单 | ✅ 已完成 | - | 与 G3 配套，支持 api_prefix |
| G5 | 登录生命周期补全：logout / refresh / 令牌吊销 / 完整个人中心 | ✅ 已完成（个人中心剩个人密钥入口） | - | logout/refresh 轮换/jti 吊销/在线用户/锁定限流/密码强度/强制登出全部完成；头像自助绑定 + 管理员代重置已落地；剩用户自助忘记密码、个人密钥入口 |
| G6 | 菜单（sys_menu）CRUD 端点 + perms 码管理界面 | ✅ 已完成 | - | 已落菜单 CRUD、按钮权限维护、当前用户动态菜单 |
| G7 | 通用分页 `src/core/pagination.py` + 查询过滤/排序/批量/脱敏 | ✅ 已完成 | - | `Page`/`PageResult` + `src/core/query.py`（`ListQuery`/`resolve_sort`/`mask_fields`/`BatchIdsRequest`）；users/roles/departments/menus/审计/配置全覆盖（树结构按设计不分页） |
| G8 | 操作日志 / 登录日志 / 在线用户 | ✅ 已完成 | - | `oper_log`/`login_log` 表 + 审计中间件 + `/admin/audit/*`；Redis 会话白名单 + `/admin/online/*` |
| G9 | 表名 `sys_` 前缀漂移同步到数据模型文档 | 文档 | 低 | 代码已统一，文档待改 |
| G10 | Pydantic 422 `msg` 的 i18n 方向 | M0 遗留 | 低 | 方向 A/B 待定 |
| G11 | 平台配置 `sys_config` 表 + 类型化缓存读取 | ✅ 已完成 | - | `sys_config` 表 + `/admin/config` CRUD + `config_accessor` 短 TTL 缓存 |
| G12 | OSS 附件基建：私有桶对象存储 + 统一上传 + 头像绑定 + 管理员代重置密码 | ✅ 已完成 | - | `core/oss.py`(+`core/image.py`) + `attach/` 统一上传(头像转码/通用附件) + `/auth/me` 头像绑定与预签名 url + `POST /admin/users/{id}/reset-password`；真实 MinIO e2e 通过；缺附件软删联动物理清理(留 M5) |
| G13 | 上传内存/解压炸弹防护 | ✅ 已完成 | - | 上传分块限额读取（`files/router.py` `_read_capped`，超 `max_bytes` 即拒，不全量入内存）+ 头像 `DecompressionBombError` 防护（`core/image.py`）；生产配 nginx `client_max_body_size` 边缘第一道闸 |
| G14 | 密码强度全路径一致 + 生产配置 fail-fast + 安全响应头 + 附件 stored-XSS 防护 | ✅ 已完成 | - | `create_user`/`update_user`/`reset_password` 统一强度校验；`config.validate_runtime` 非 local 缺 JWT key/开 debug/CORS 通配/信任代理跳数<1 即拒启动；`SecurityHeadersMiddleware`；通用附件预签名强制 `Content-Disposition: attachment` |
| G15 | ARQ 接对专用 Redis 库 | ✅ 已完成 | - | `tasks/worker.py` 改用 `RedisSettings.from_dsn(redis_arq_url)`（db 1），与业务缓存 db 0 隔离 |
| G16 | 文档漂移修复：超管判定 / PUBLIC_PATHS / 测试数 | ✅ 已完成 | - | §4.6 超管由 `superadmin` role code 承载（非 `*:*:*`）；§4.3 PUBLIC_PATHS 补 `/auth/refresh`；全量测试 693 passed / 覆盖率 98% |
| G17 | 指标监控（Prometheus/OTel）+ 通用接口限流 | M2 / 边缘层 | 中 | **刻意推迟**：指标随 M2 热路径上量统一接入（横切观测基建，第一个真实消费者再引入）；通用限流留 nginx 边缘层（反代职责，不在应用层重造）。详见 §6.1 |
| G18 | 登录日志写放大 + JWT iss/aud | M2 / 后续 | 低 | **刻意推迟**：锁定后仍写 login_log 在 2000 员工规模可接受，随 M2 用量上量再评估异步化/降采样；JWT iss/aud 仅在未来多服务共享密钥时才有意义，当前 RS256 硬锁已防算法混淆（真正攻击面），不提前堆砌。详见 §6.1 |

### 6.1 刻意推迟项（非地基缺口，记录理由防遗忘）

以下经 Oracle 审计识别、但**刻意不在本轮落地**，因其违反「无消费者的基建是过度工程」或属边缘层职责，不阻塞业务开工：

- **指标监控（Prometheus / OpenTelemetry）**：是横切观测基建。M2 网关热路径已落地，指标接入时机已到——随网关**真实上量部署**时统一接入（DB/Redis pool、接口耗时、登录失败、上游调用等指标在有真实流量时才有意义）。现已有结构化日志 + trace_id + 健康探针兜底可观测性。
- **通用接口限流（登录外）**：架构定位 nginx 为反代 + 负载均衡层，通用限流是**边缘层职责**，应在 nginx（`limit_req`）落地，不在应用层重造一套。登录维度限流（按用户名锁定 + 按 IP 滑窗）因涉及业务语义（防爆破 + 防枚举）已在应用层实现。
- **登录日志写放大**：登录被锁定后仍写 login_log 留痕（安全审计需要）。2000 员工规模下写量可接受；随 **M2** 整体用量上量后再评估「锁定后降采样 / 审计异步化」。
- **JWT `iss` / `aud` claims**：仅在「多服务共享同一签名密钥」场景才防 token confusion；本平台 JWT 仅自签自用，且 RS256 已硬锁（`decode` 显式传 `algorithms=["RS256"]`）防住真正的算法混淆攻击面。加 iss/aud 是低收益的契约变更，留待真有多服务消费者时再做。

---

## 七、全局约束（贯穿所有里程碑）

- **类型安全零容忍**：禁 `# type: ignore`，`mypy --strict` 是 CI 门禁。
- **测试是收工前置**：新功能/修 bug 必带测试（红 → 绿）；禁删/skip 失败用例骗 CI；单测不碰共享 PG/Redis（用内存 SQLite + 纯异步 Redis double）。
- **四层单向依赖**：router → service → repository → db；router 薄、service 厚。
- **窄腰 + 最小依赖**：新依赖必带版本上界；新基建第一个真实消费者出现再引入（无消费者的基建是过度工程）。
- **prompt 缓存神圣**（网关侧）：不中途改上下文/换工具集/重建系统提示。
- **i18n 纯 code**：后端只发 code，前端拥有展示文本；枚举 label 后端单源 + codegen 同步；不建 DB 字典表。
- **配置/密钥分离**：`.env` 仅放密钥；行为配置走 `config.py`。
- **关联无物理外键**：存 id + 普通索引，引用完整性由 service 维护（数据模型 0.7）。
